<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.config.RoomMapper">
     <!-- 사원 저장 -->
   <insert id="save_R" parameterType="RoomDTO">
      insert into tb_room(room_no,room_state,room_capacity,room_checkin_time,room_checkout_time)
      values( #{room_no},1,#{room_capacity},now(),now())
   </insert>
   
   <delete id="remove_R" parameterType="int" >
      delete from tb_room
      where room_no =#{room_no}
   </delete>
   
   <!-- 수정 -->
     <update id="update_R" parameterType="RoomDTO" >
        update tb_room
        room_state = #{room_state} ,
        room_capacity = #{room_capacity} , 
        room_checkin_time = #{room_checkin_time} ,
        room_checkout_time = #room_checkout_time}
        where room_no=#{room_no}
     </update>
     
   <!--전체 조회 -->
   <select id="find_R12" resultType="RoomDTO">
    <![CDATA[
        select room_no, 
               room_state, 
               room_capacity, 
               room_checkin_time, 
               room_checkout_time
        from tb_room
        where room_capacity < 2 
        order by room_no desc
    ]]>
</select>
   <select id="find_Q" resultType="RoomDTO">
        select room_no, 
               room_state, 
               room_capacity, 
               room_checkin_time, 
               room_checkout_time
        from tb_room
</select>
   <select id="find_R34" resultType="RoomDTO">
    <![CDATA[
        select room_no, 
               room_state, 
               room_capacity, 
               room_checkin_time, 
               room_checkout_time
        from tb_room
        where room_capacity < 5
        and room_capacity > 2 
        order by room_no desc
    ]]>
</select>
   
   <select id="find_R56" resultType="RoomDTO">
    <![CDATA[
        select room_no, 
               room_state, 
               room_capacity, 
               room_checkin_time, 
               room_checkout_time
        from tb_room
        where room_capacity >4
        and room_capacity<7
        order by room_no desc
    ]]>
</select>
</mapper>

